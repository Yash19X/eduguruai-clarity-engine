<!DOCTYPE html>
<html>
<head>
  <title>EduGuruAI Clarity Engine Demo</title>
</head>
<body>

<h2>EduGuruAI – Human Intelligence OS</h2>

<textarea id="input" rows="4" cols="50"
  placeholder="Type your confusion here..."></textarea>
<br><br>

<button onclick="runEngine()">Get Clarity</button>
<button onclick="showHistory()">View Decision History</button>

<pre id="output"></pre>
<div id="history" style="margin-top:20px;"></div>

<!-- 1️⃣ Emotion Engine (optional) -->
<script src="src/emotion-engine/emotionEngine.js"></script>

<!-- 2️⃣ Clarity Engine -->
<script src="src/clarity-engine/clarityEngine.js"></script>

<!-- 3️⃣ Queue Engine (depends on clarity engine) -->
<script src="src/queue-engine/queueEngine.js"></script>

<!-- 4️⃣ Decision Memory -->
<script src="src/decision-memory/decisionMemory.js"></script>

<!-- 5️⃣ RUN ENGINE & HISTORY -->
<script>
  function runEngine() {
    const text = document.getElementById("input").value;

    // Queue Engine call karo
    const result = queueEngine(text, clarityEngine);

    // Decision Memory me save
    saveDecision(text, result);

    // Output show karo
    document.getElementById("output").textContent =
      JSON.stringify(result, null, 2);

    // History update
    showHistory();
  }
</script>

</body>
</html>